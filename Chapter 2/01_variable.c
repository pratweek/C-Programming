#include<stdio.h>

int main (){
    int i = 10; // i again cannot be used for storing any other value 
    int j = i;
    int a = 4, b = 6, c=5, d =3;


    printf("%d %d\n", i,j);
    printf("%d %d\n", a,b);
    printf("%d %d\n", c,d);
    // %d is called format specifier
    // %c represents char, %f represents float
    int p, q,r,s;
    p=q=r=s=23;  


    return 0;
}