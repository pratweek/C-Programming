#include<stdio.h>

int main (){
   /* char st[] = "abc" ; // no need for null character
    for (int i = 0; i < 3; i++)
    {
        printf("%c", st[i]);
    }
    
    printf("first character is %c\n", st[0]);  */

    char st[] = "abc";
    printf("%s",st);
    return 0;
}